class Solution {
    /**
     * @param {string[]} strs
     * @return {string[][]}
     */

    groupAnagrams(strs) {
        // for each string
        // find anagrams comparing every other string
        // strings with the same number
    }
}
